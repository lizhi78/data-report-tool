## 上周收尾1
1. 两个仓库的合并(work-log和data-report-tool)具体操作如下：
    1. GitHub 网页 → 找到对应需要clone的仓库 → Download ZIP
    2. 解压后复制文件到目标仓库文件夹
    3. git add . → git commit → git push
    4. 这样做可以绕过网络超时问题
    5. 在这个过程中，实习日志等.md文件一直没有出现，后面发现在.gitignore文件中有忽略.md文件的配置，所以修改了.gitignore文件，变为output/*.md只忽略output文件夹下的.md文件
2. 补齐ai_report.py和exporter.py的代码以及.gitignore文件
3. 补全README.md文件,将生成结果的.md和excel文件作为真实输出贴上
4. output文件夹下的.md文件和excel文件：
[text(markdown)](../output/汇总_实习练习-销售脏数据.md)
[text(excel)](../output/汇总_实习练习-销售脏数据.xlsx)